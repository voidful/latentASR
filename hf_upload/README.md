# HF Upload Helper

Run from the repository root:

```bash
python hf_upload/upload_to_hf.py --repo-id voidful/latentASR
```

The script uploads the entire project folder, including the released adapter
checkpoint and documentation, to a Hugging Face model repository.

